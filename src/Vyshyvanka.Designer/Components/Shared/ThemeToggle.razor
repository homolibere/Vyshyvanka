@namespace Vyshyvanka.Designer.Components

<button class="theme-toggle" @onclick="Toggle" title="@(ThemeService.IsDark ? "Switch to light theme" : "Switch to dark theme")" aria-label="Toggle theme">
    @if (ThemeService.IsDark)
    {
        <i class="fas fa-sun"></i>
    }
    else
    {
        <i class="fas fa-moon"></i>
    }
</button>

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>FlowForge.Plugin.GitLab</RootNamespace>

    <!-- NuGet Package Metadata -->
    <PackageId>FlowForge.Plugin.GitLab</PackageId>
    <Version>1.0.0</Version>
    <Authors>FlowForge</Authors>
    <Description>GitLab integration plugin for FlowForge. Manage issues, files, releases, and repositories.</Description>
    <PackageTags>flowforge;plugin;gitlab;git;devops</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryType>git</RepositoryType>

    <!-- Required for FlowForge plugin loading -->
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>

    <!-- XML docs for IntelliSense -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- FlowForge.Core is provided by the host at runtime — do not bundle it -->
    <PackageReference Include="FlowForge.Core">
      <Private>false</Private>
      <ExcludeAssets>runtime</ExcludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <None Include="../README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>

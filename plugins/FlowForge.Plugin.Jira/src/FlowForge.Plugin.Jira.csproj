<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>FlowForge.Plugin.Jira</RootNamespace>

    <!-- NuGet Package Metadata -->
    <PackageId>FlowForge.Plugin.Jira</PackageId>
    <Version>1.0.0</Version>
    <Authors>FlowForge</Authors>
    <Description>Jira Software integration plugin for FlowForge. Create, update, and manage issues, comments, and users.</Description>
    <PackageTags>flowforge;plugin;jira;atlassian;project-management</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryType>git</RepositoryType>

    <!-- Required for FlowForge plugin loading -->
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>

    <!-- XML docs for IntelliSense -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- FlowForge.Core is provided by the host at runtime — do not bundle it -->
    <PackageReference Include="FlowForge.Core">
      <Private>false</Private>
      <ExcludeAssets>runtime</ExcludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <None Include="../README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
